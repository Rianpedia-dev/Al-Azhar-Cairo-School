// src/lib/db.ts per PRD 4.2
// Database client placeholder/instance for Prisma ORM (MySQL 8.x)

// Global database accessor for server-side Prisma operations
export const db = {
  user: {
    findMany: async () => [],
    findUnique: async () => null,
  },
  kelas: {
    findMany: async () => [],
  },
  tugas: {
    findMany: async () => [],
  },
  prestasi: {
    findMany: async () => [],
  },
};
